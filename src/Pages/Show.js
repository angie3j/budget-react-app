import ShowDetails from "../Components/ShowDetails"; 

function Show() {
    return (
        <div className="ShowDetails">

            <h2>Bank Account Total:</h2>
            <ShowDetails />

        </div>
    );
};

export default Show;