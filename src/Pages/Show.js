import ShowDetails from "../Components/ShowDetails"; 

function Show() {
// function Show(props) {
    // console.log(props)
    return (
        <div className="ShowDetails">

            <ShowDetails />
      
        </div>
    );
};

export default Show;